# Q 表蒸餾式 DQN：使用情境

---

## 系統行為總覽

RR 平台有兩個獨立的選擇，彼此不互相影響：

- **演算法**（左側 UI）：控制 Agent 怎麼學
- **繪製資料**（分析頁）：控制熱力圖畫什麼

---

## 演算法行為

| | Qt 模式 | DQN 模式 |
|--|--|--|
| **Q-Table 更新** | 每步 SAR → Bellman，max Q(s') 查自己 | 每步 SAR → Bellman，max Q(s') 從 NN 取 |
| **動作選擇估值** | Q-Table 查表 | NN predict |
| **NN fit 時機** | 背景持續迴圈，不中斷，不影響遊戲 | 每回合結束 await fit，fit 完才送下一局第一步 |
| **遊戲暫停時 NN** | 背景迴圈繼續跑，NN 持續向 Q-Table 學 | 自動啟動追趕蒸餾，持續 fit 直到恢復 |

> `dqnFitted` 保護機制：第一次 fit 完成前，DQN 模式仍退回查 Q-Table，避免亂數估值污染學習。

---

## 繪製資料行為

| | 價值表格（Q-Table） | 價值網路（DQN） |
|--|--|--|
| **資料來源** | QTable 直接查表 | NN 對整個狀態空間的批量推論 |
| **覆蓋範圍** | 只有探索過的格子 | 整個狀態空間（含未探索區域） |
| **更新頻率** | 每秒重繪 | 每秒 batchPredict + fitDone 後立即刷新 |

---

## 使用情境

### 情境 A：觀察 NN 跟上 Q-Table 的過程

- 演算法：Qt
- 繪製資料：DQN

Agent 跑遊戲，Q-Table 每步更新，NN 在背景一輪接一輪追趕。
熱力圖顯示 NN 對整個狀態空間的預測——包含 Agent 從未走到的地方。
可以觀察到隨著訓練推進，NN 的熱力圖逐漸長出 Q-Table 的形狀。

---

### 情境 B：手動讓 NN 追上（暫停蒸餾）

- 演算法：任意
- 繪製資料：DQN

訓練一段時間後按暫停，Qt 停止更新，NN 繼續蒸餾。
熱力圖每秒刷新，可以即時看到 NN 收斂的過程。
這是「人工讓 Qt 等 NN 趕上」的操作——讓兩者盡量對齊後再繼續訓練。

---

### 情境 C：切換 DQN 模式，讓泛化能力反哺 Q-Table

- 演算法：DQN
- 繪製資料：任意

切換後，Q-Table Bellman 更新的 max Q(s') 改從 NN 取。
NN 的空間泛化能力讓 Q-Table 能更新它從未走到的狀態——雙向互學正式啟動。
Q-Table 熱力圖此時可能出現孤立的亮格，周圍全是空白：
這是 Q-Table 借用 NN 想像出來的估值，寫進自己記憶的痕跡。

---

## 完整行為矩陣

| | 繪製資料：Q-Table | 繪製資料：DQN |
|--|--|--|
| **Qt 模式，遊戲運行中** | 有探索才有顏色，每步更新 | NN 背景持續 fit，每秒刷新推論 |
| **Qt 模式，遊戲暫停中** | 靜止（Qt 無新 SAR） | NN 繼續蒸餾，熱力圖每秒更新 |
| **DQN 模式，遊戲運行中** | 有探索才有顏色，每步更新 | 每回合末 fit 後刷新 |
| **DQN 模式，遊戲暫停中** | 靜止（DQN 亦無新 SAR） | 自動追趕蒸餾，熱力圖每秒更新 |

---

*設計動機與架構見 `Q表蒸餾式DQN：設計心法.md`*
*實作細節見 `dqnWebWorker.js` 與 `index.html`*
