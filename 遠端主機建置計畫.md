# 遠端主機建置計畫

> 目標：把 DESKTOP-J17AJFD 建置成可被 Claude 遠端調用的自動化引擎。
> 硬體：i5-12400F / RTX 2060 6GB / 16GB RAM / 1.38TB / Windows 11

---

## 定位

這台主機不是給人用的，是給 Claude 用的。
Claude 透過 Flask Worker API 送出任務，主機執行後回傳結果。

```
Claude Code
    ↓  HTTP POST
Flask Worker（主機）
    ↓  subprocess
Python 腳本 / Playwright / PyTorch / ffmpeg
    ↓
stdout / stderr / 截圖 / CSV / 影片
    ↑  HTTP GET
Claude Code
```

---

## 一、Flask Worker（核心基礎）

**所有功能的前提，第一個建。**

### API 規格

| Endpoint | 說明 |
|----------|------|
| `POST /jobs` | 建立任務（送入 Python 腳本 + timeout） |
| `GET /jobs/<id>` | 查詢任務狀態 |
| `GET /jobs/<id>/logs` | 取得 stdout / stderr |
| `GET /jobs/<id>/artifacts` | 取得輸出檔案列表 |
| `GET /jobs/<id>/artifacts/<file>` | 下載特定輸出檔 |
| `POST /run_code` | 短任務同步執行（10 秒內直接回傳） |

### 目錄結構

```
C:/worker/
    app.py              Flask 主程式
    worker_jobs/
        <job_id>/
            main.py
            stdout.txt
            stderr.txt
            status.json
            artifacts/
                result.json
                screenshot.png
                output.mp4
```

### 安全設計
- Bearer Token 驗證（每個 request 必須帶）
- 每個 job 有 timeout（預設 300 秒，最長 3600 秒）
- 同時最多 2 個 job 並行（避免資源衝突）
- 腳本只能在 `worker_jobs/<id>/` 下讀寫，不能存取任意路徑

### 對外方式
- Cloudflare Tunnel（推薦）：不需要開 port，不暴露 IP
- 或 ngrok：快速測試用

---

## 二、Headless 自動化任務（Playwright）

建在 Flask Worker 上，適合：

- **RR 平台巡檢**：自動開啟每個遊戲、訓練 N 回合、截圖、檢查有無 console error
- **論文批次實驗**：同一遊戲跑不同超參數，匯出 reward CSV
- **定期截圖存檔**：訓練過程的 Q-Table 熱力圖、reward 曲線截圖

特性：
- `headless=True`，不需要螢幕
- viewport 固定，截圖解析度穩定
- canvas / Plotly 圖表都截得到

---

## 三、Local LLM（Ollama）

RTX 2060 6GB VRAM 可跑的模型：

| 模型 | VRAM | 用途 |
|------|------|------|
| Gemma 3 4B Q4 | ~3GB | 快速分析、摘要 |
| Qwen2.5 7B Q4 | ~5GB | 較強推理、論文潤稿 |

建置方式：
- 安裝 Ollama，開啟 API server（`localhost:11434`）
- Flask Worker 加一個 `/llm` endpoint，代理 Ollama 請求
- Claude 透過 worker 呼叫本地模型，不消耗 API 費用

適合用途：
- 批次分析 RR 訓練 log，自動產報告
- 論文段落潤稿、中英翻譯
- 分析 btc_trading 的策略行為

---

## 四、BTC CNN 定期重訓排程

RTX 2060 跑 PyTorch CNN 完全夠用。

### 流程
```
每天凌晨（Windows 工作排程器）
    ↓
爬取最新 BTC 1h K 棒（ccxt / binance API）
    ↓
重跑 CNN 訓練（PyTorch）
    ↓
輸出 btc_states.json
    ↓
git push 到 ReinforceLab repo
    ↓
Cloudflare Pages 自動部署，RR 平台資料更新
```

效果：btc_trading.html 的資料永遠是最新的，不需要手動跑 Colab。

---

## 五、錄影 / 直播（需 RDP 長駐）

**注意：錄影和直播無法 headless，需要桌面環境。**

### 建置方式
- Windows RDP 長駐 session（不需接螢幕，遠端桌面就是桌面）
- Playwright `headless=False` 開啟 RR
- ffmpeg 錄製視窗畫面

### 用途
- 自動錄製教學示範影片（訓練過程 + 圖表變化）
- 直播 RR 訓練過程（搭配 OBS 虛擬場景）
- 遠端控制 RR 展示，不需要人在場

> 直播控制建議另外做成 `control worker`（長駐 session + 接收指令），
> 而不是一次性 job，設計上有差異，後續再規劃。

---

## 六、建置順序

```
Step 1  Flask Worker 基礎版
        - POST /jobs、GET /jobs/<id>、GET /jobs/<id>/logs
        - Bearer Token 驗證
        - Cloudflare Tunnel 對外
        → 完成後 Claude 就能調用主機執行任意 Python 腳本

Step 2  Playwright headless 支援
        - 安裝 Playwright + chromium
        - 測試：headless 開 RR、截圖、回傳
        → 完成後可做 RR 巡檢與論文實驗

Step 3  Ollama + Local LLM
        - 安裝 Ollama，跑 Qwen2.5 7B Q4
        - Flask Worker 加 /llm 代理
        → 完成後可離線分析 log、潤稿

Step 4  BTC CNN 排程
        - 爬蟲 + 重訓腳本
        - Windows 工作排程器設定每日執行
        → 完成後 btc_states.json 自動更新

Step 5  RDP 長駐 + 錄影
        - 設定 RDP 自動登入 session
        - ffmpeg 錄影腳本
        → 完成後可錄製教學影片
```

---

## 七、這套系統對論文的直接價值

你的論文研究 RR 平台在 RL 教學的成效，這台主機可以：

1. **自動跑對照實驗**：不同超參數 × 多個遊戲 × 重複 N 次，全自動
2. **客觀紀錄學習曲線**：reward、steps、收斂速度，存成 CSV 直接畫圖
3. **截取關鍵畫面**：Q-Table 熱力圖收斂前後對比，直接用進論文
4. **驗證平台穩定性**：巡檢腳本確認每個遊戲在不同超參數下都能正常跑

---

## 八、預計對外 URL 結構

```
https://worker.leaflune.org/jobs          POST  建立任務
https://worker.leaflune.org/jobs/<id>     GET   查詢狀態
https://worker.leaflune.org/jobs/<id>/logs      GET   取得 log
https://worker.leaflune.org/run_code      POST  短任務同步
https://worker.leaflune.org/llm           POST  本地 LLM 代理
```

Token 存在 Claude 的 memory，每次呼叫自動帶入。
